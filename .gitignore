# build / cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.hypothesis/
pytest-cache-files-*/
.coverage
htmlcov/
*.egg-info/
build/
dist/

# venv
.venv/
venv/

# data / logs / reports (do not commit)
# NOTE: anchored to repo root so the src/tickweaver/data/ source package is
# NOT ignored (previously a bare `data/` rule hid the whole module from git).
/data/
logs/
reports/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# env
.env
.env.local

# plan / design notes
plans/
plan.md
plan_viz.md

!strategies/short_demo.py
!strategies/future_demo.py

# private strategy (Vulture) — kept local only, never committed
strategies/vulture.py
configs/vulture.yaml

# diagnostic logs from `--viz 2> ...log` redirect
*_diag.log
vulture_diag.log
